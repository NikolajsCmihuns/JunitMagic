Write tests for two domains:
1) Database:
        DatabaseFacadeImpl
2) Validation:
        EmptyRule
        PasswordRule
        UserPolicy

Use Mockito to mock all dependencies of class under test.